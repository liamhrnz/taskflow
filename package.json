{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "TaskFlow - A transactional task management web application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "server:json": "json-server --watch db.json --port 3000",
    "lint": "eslint js/ --max-warnings 0",
    "test": "echo \"Run manual tests via TEST_REPORT.md\""
  },
  "keywords": ["task", "manager", "transactional", "rest-api", "express"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
